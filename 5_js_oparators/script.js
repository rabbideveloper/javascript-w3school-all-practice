/**
 * ! JavaScript Operators
 *
 * ? Javascript operators are used to perform different types of mathematical and logical computations.
 *
 ** Examples:
    *! The Assignment Operator = assigns values

    *! The Addition Operator + adds values

    *! The Multiplication Operator * multiplies values

    *! The Comparison Operator > compares values
 */

/**
 * ! JavaScript Assignment
 *? The Assignment Operator (=) assigns a value to a variable:
 */

//todo :  Assignment Example:-

// let x = 10;

// Assign the value 5 to x
let x = 5;

// Assign the value 2 to y
let y = 2;

/**
 * ! JavaScript Addition
 *? The Addition Operator (+) adds numbers:
 */

// Assign the value x + y to z:
let z = x + y;

// console.log(z);
// document.getElementById("demo").innerHTML = z;

/**
 * ? JavaScript Multiplication
 *! The Multiplication Operator (*) multiplies numbers:
 */

let firstMultNum = 10;
let secondLastNum = 9;

let multNum = firstMultNum * secondLastNum;
console.log(multNum);

document.getElementById("demo").innerHTML = multNum;

/**
*! Operator	        Example	        Same As
   =	            x = y	        x = y
   +=	            x += y	        x = x + y
   -=	            x -= y	        x = x - y
   *=	            x *= y	        x = x * y
   /=	            x /= y	        x = x / y
   %=	            x %= y	        x = x % y
   **=	            x **= y	        x = x ** y
*/

/**
 *! JavaScript Comparison Operators
*? Operator	          Description
    ==	                equal to
    ===	                equal value and equal type
    !=	                not equal
    !==	                not equal value or not equal type
    >	                greater than
    <	                less than
    >=	                greater than or equal to
    <=	                less than or equal to
    ?	                ternary operator
 */

let text1 = "What's a nice";
text1 += " day it is";

document.getElementById("demo").innerHTML = text1;

let firstNum = 10;

firstNum += firstMultNum + 20;

document.getElementById("demo").innerHTML = firstNum;

let age = 23;

age += 20;

console.log(age);

/**
 * ! JavaScript Logical Operators
*?  Operator	Description
    &&	        logical and
    ||	        logical or
    !	        logical not
 */
